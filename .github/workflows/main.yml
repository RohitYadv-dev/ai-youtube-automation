on:
  schedule:
    - cron: '0 8 * * *'      # ← Daily at 8 AM UTC
  workflow_dispatch:          # ← Manual trigger
  push:
    branches:
      - main
      - master               # ← Trigger on code push
